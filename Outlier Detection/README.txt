The Rshiny web app shows different methods to detech outliers in a dataset.

Finding outliers, deciding whehter they are really an outlier is import in data analysis.

The perfomance of a model is hugely affected by outliers.

